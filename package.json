{
  "name": "code-weaver",
  "version": "2.0.0",
  "description": "3D code visualization tool with dead code detection",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python app.py",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "start": "python app.py",
    "install:all": "npm install && cd frontend && npm install",
    "clean": "cd frontend && rm -rf dist node_modules && npm install",
    "test": "python -m pytest && cd frontend && npm run type-check",
    "format": "black src/ && cd frontend && prettier --write src/",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "python": ">=3.9.0"
  }
}